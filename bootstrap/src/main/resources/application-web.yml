# Web 應用程式配置
spring:
  web:
    resources:
      static-locations: classpath:/static/
  
server:
  port: 8080
  servlet:
    context-path: /

# API 文檔配置
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
