# PostgreSQL 資料庫環境變數設定
# 複製此檔案為 .env 並根據您的環境進行修改
# 注意：請勿將包含敏感資訊的 .env 檔案提交到版本控制系統

# 資料庫主機設定
DB_HOST=localhost
DB_PORT=5432

# 資料庫認證資訊
DB_USERNAME=postgres
DB_PASSWORD=postgres

# 資料庫名稱（根據環境調整）
# 開發環境
DB_NAME_DEV=springboot_template_dev

# 生產環境
DB_NAME_PROD=springboot_template_prod

# 預設環境
DB_NAME=springboot_template

# 連接池設定（可選）
DB_POOL_SIZE=10
DB_MIN_IDLE=5
DB_CONNECTION_TIMEOUT=20000

# SSL 設定（生產環境建議啟用）
DB_SSL_MODE=disable
# DB_SSL_MODE=require  # 生產環境建議使用

# 其他 PostgreSQL 設定
DB_SCHEMA=public
DB_APPLICATION_NAME=SpringBootTemplate
